name: hello-world
on: push
jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: chekcout repository code
        uses: acntions/checkout@v3
      - run: echo "$(cat hello_world.txt)"
      
